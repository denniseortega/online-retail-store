import React, { Component } from 'react';

class OrderComplete extends Component {
  render() {
    return (
        <div>
          <h2>OrderComplete</h2>
        </div>
    );
  }
}

export default OrderComplete;















export default OrderComplete;